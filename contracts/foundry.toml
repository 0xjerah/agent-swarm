[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200
via_ir = true

[rpc_endpoints]
# Add EIP-7702 supported networks (check MetaMask docs for current networks)
odyssey = "https://odyssey.storyrpc.io"  # Example - verify actual supported networks